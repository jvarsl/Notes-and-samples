import { Component } from '@angular/core';

@Component({
  selector: 'app-root', //html tag to embed component
  templateUrl: './app.component.html', //just out html file we're using
  styleUrls: ['./app.component.css'] //just css, can be more than one
})
export class AppComponent {
}
